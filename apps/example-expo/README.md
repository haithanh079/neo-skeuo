# example-expo

Minimal runnable demo for `@neo-skeuo/react-native` (Alpha).

```bash
cd apps/example-expo
npm install
npm start
```

See [AGENTS.md](../../AGENTS.md) — React Native package is Alpha; web admin should use `@neo-skeuo/refine` instead.
