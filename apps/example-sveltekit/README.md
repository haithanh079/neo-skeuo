# example-sveltekit

Minimal runnable demo for [docs/recipes/svelte.md](../../docs/recipes/svelte.md).

```bash
cd apps/example-sveltekit
npm install
npm run dev
```

Uses `@neo-skeuo/svelte` SFC subpaths and `@neo-skeuo/web-css/index.css`.
